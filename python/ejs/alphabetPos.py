def alphabet_position(text):
    listStr = list(text)
    for char in enumerate(listStr):
        
    pass


# for	Iterar sobre una secuencia directamente
# range()	Generar una secuencia de números para iterar
# enumerate()	Iterar sobre una secuencia obteniendo índice y valor
# Si necesitas números, usa range().
# Si necesitas índices y valores, usa enumerate().
# Si solo necesitas valores, usa for directamente. 